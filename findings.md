# 发现与决策：TAGTAG 竞品调研

## 2026-08-09 全局备份恢复与发布

- 当前完整备份创建包含 SQLite 快照、标签状态、资源层级和 v1 manifest，但 manifest 没有文件哈希，也没有灾难恢复入口。
- 控制器现有 `restoreBackup` 读取的是早期单文件 JSON 标签状态，不能恢复 SQLite、操作日志或资源字节；必须与“全局备份恢复”分开命名和实现。
- 全局恢复首版只允许写入新的空存储根；当前资料库继续可用，直到新资料库完整校验并恢复成功后才切换配置。
- GitHub Actions 当前只压缩 Release 目录生成便携 ZIP；后续标签发布必须额外生成 Windows 安装程序并同时上传两个产物。
- 完整备份 v2 已精确列出 `metadata/` 与 `resources/` 下所有文件和目录，并校验路径安全、类型、大小、SHA-256、SQLite 完整性、资源清单及标签状态。
- 恢复流程只接受新的空目录，在同级临时目录重建并重新打开校验后才原子切换；应用协调器在失败时恢复旧标签状态、设置和存储根配置。
- 发布 workflow 使用同一 AppId 连续静默安装两次来覆盖原地升级路径，再静默卸载；便携目录、安装目录和升级后目录都复用原生插件打包冒烟检查。
- 发布配置回归测试使用 Dart YAML 解析器读取 workflow，并让 PowerShell AST parser 逐段解析所有 `pwsh` 块；同时校验 Inno Setup 与 Windows runner 使用同一个单实例互斥锁。
- GitHub 默认分支 `main` 当前仍只有初始提交，Actions 页面不会识别仅存在于功能分支的 `workflow_dispatch`；需先合并当前分支到默认分支，或由明确的 `v*` 标签触发标签构建。

## 2026-08-09 一致性告警处理

- 外部移动只能由用户显式配对一个缺失资源记录和一个未跟踪路径；首版不依据文件名、大小或修改时间自动推断身份。
- “接受新路径”只更新原资源记录的路径和元数据，不移动文件；“恢复记录路径”才把候选内容搬回原记录位置。两者都保留资源 ID、空间成员、标签关系和历史，并支持撤销。
- 未跟踪内容可以直接接管，也可以移动到存储根之外；接管撤销后保留原始内容并重新成为未跟踪，移出撤销后搬回原位置但不自动接管。
- 所有移动与撤销都先检查目标空闲，绝不覆盖；文件事务使用复制、元数据事务、删除和失败补偿保证双方内容不因中途失败而丢失。
- 一致性扫描现在同时刷新普通编辑后的大小与修改时间，并持久更新 `managed / missing` 状态；正常内容编辑不产生告警。
- 告警对话框在原位置执行并刷新四类动作；缺失资源没有候选时继续显示异常删除状态，不静默清理记录。

## 2026-08-09 导航与资源视图语义校正

- 用户明确指出左侧导航折叠只是即时布局状态，不属于设置项；展开仅补充入口文字和必要控件，不能改变当前页面语义。
- “标签层级”是与全部资源、待整理、最近并列的独立主内容页，应按标签树递归展开，显示每个标签的资源数量及该层级对应资源，不能用点击它来展开侧栏。
- 顶部品牌下方的常驻搜索框与左栏搜索入口重复；搜索应由独立页面承载。文件与文件夹导入保持两个直接命令，并与标签空间切换、新建空间等全局命令排列在右上角。
- 默认资源点击语义是单选；批量选择必须由显式“多选模式”命令进入，避免连续单击无意累积选择。
- 当前检查器没有预览价值，首版移除；打开、定位、添加标签、清除直接标签、恢复并退出管理改为资源行内直接动作。
- 当前普通资源行与复选框共用 `toggleResourceSelection`；该接口只对 `selectedResourceIds` 集合增删，不会先清理旧项。这直接解释连续单击累积多选，修复应新增单选命令并仅在显式多选模式中保留 toggle。
- 现有视图状态由 `activePlacementId`、`showRecent`、`showInbox` 三套字段组合，导致“标签层级入口”和导航展开容易耦合。阶段 11 应收敛为明确的主内容视图枚举，`activePlacementId` 只保留标签实体操作上下文。
- 当前展开侧栏同时承载标签空间下拉框和完整标签树；用户校正后这两者都应离开侧栏。展开态只将六个导航图标补充文字，标签空间切换进入右上角，标签树进入独立主内容页。
- 唯一标签的多个位置共享 `tagId`；标签层级页的节点资源数量和列表必须按 `tagId` 汇总直接标注，而不是只按当前 `placementId`，否则同一唯一标签在不同路径下会显示不同集合。
- 当前资源表操作列固定 76px，只容纳打开与定位。迁入添加标签、清标签、恢复退出后需要稳定约 180px 的五图标操作列，并在紧凑宽度下隐藏标签列而非折叠这些关键命令。
- Release 视觉验证确认 64px 折叠导航和 220px 展开导航都保持页面状态；展开只增加六个入口文字，标签层级页不会再由导航宽度切换隐式触发。
- 标签层级页面已验证可显示节点资源数、唯一标签链接标识、独立展开箭头和对应资源行；960px 紧凑布局会隐藏直接标签列，但五个关键资源动作和右上角全局命令仍完整可见。
- 真实连续点击验证普通模式只高亮后一项；显式进入多选模式后才显示复选框，并能同时选中两项，证明 UI 调用链与领域测试一致。
- 搜索框已验证只存在于独立搜索页面；设置弹窗已移除导航折叠项，旧 `preferences.json` 中可能存在的额外字段会被兼容忽略。

## 2026-08-09 文件打开与主界面调整

- 用户实际存储根中的三个 TXT 资源都真实存在且可读取，三者均带 Nextcloud reparse point 属性；因此不能仅凭 reparse point 解释“只有 `test.txt` 能打开”。
- 当前 Windows 打开适配器把绝对路径转换为 `file://` URI，再调用 `rundll32.exe url.dll,FileProtocolHandler`；实际文件名包含空格和中文，反馈环必须覆盖这些真实路径，不能只使用现有的 ASCII 临时文件假启动测试。
- 最小矩阵确认空格不是原因：`alpha beta.txt` 通过当前 URI 通道可打开，而普通临时目录中的 `副本.txt` 和 `测试 副本.txt` 均无法打开。根因是 `FileProtocolHandler` 未正确处理 `Uri.file()` 产生的百分号编码中文路径。
- `explorer.exe <原始绝对路径>` 对用户存储根中的三个真实 TXT 均启动了名称匹配的 Notepad 窗口，可作为不改变文件关联语义的修复通道。
- 当前资源行只有“打开”和“定位”两个动作，却隐藏在三点菜单中；用户要求改为行内直接图标。左侧导航的目标一级入口固定为全部资源、标签层级、待整理、最近、搜索、设置，并支持文字折叠。
- UI 重构采用 64px 折叠图标列与 276px 展开栏；标签树只在展开状态呈现，搜索入口聚焦顶部搜索框。设置首批持久化默认导入方式与导航折叠状态，并展示当前存储根，避免只有按钮没有可用设置。
- “清除全部直接标签”从顶部批量命令和检查器大按钮移除，降级到“直接标签”标题旁的小图标并继续要求确认；资源行的打开与定位则提升为两个始终可见的图标按钮。
- Release 窗口在 1280×720 与 960×720 的展开、折叠导航下均无重叠或文本溢出；窄窗口自动隐藏检查器与直接标签列，但保留打开、定位两个关键操作。
- 设置中的导航折叠状态经过保存、关闭和重新启动后仍生效；验收结束后已恢复为展开默认。三个真实 TXT 也均通过资源行双击打开了名称匹配的 Notepad 窗口。

## 2026-08-09 Windows 运行时与后续开发发现

- Flutter Windows 使用 `--no-pub` 构建时会跳过平台插件注册文件刷新；依赖变化后必须先正常执行 `flutter pub get`，Release 产物还应显式检查原生插件 DLL。
- Windows 进程可能同时拥有输入法/UAC 覆盖窗口和 Flutter 主窗口；自动化与单实例激活必须按 `FLUTTER_RUNNER_WIN32_WINDOW` 窗口类定位，不能依赖 `Process.MainWindowHandle`。
- 阶段 9 的资源退出管理可继续使用已确认的 `ManagedLibrary` 公共测试 seam；现有导入日志与撤销只支持 `import_copy/import_move`，操作模型和 SQLite schema 需要版本化扩展。
- schema v1 的 `operations.type` 使用只允许导入类型的 SQLite `CHECK` 约束；新增退出操作必须迁移并重建该表，不能只修改 Dart 枚举或把旧资料库直接标记为新版本。

## 用户需求
- 对文件和文件夹采用统一的可标注对象模型。
- 软件后台运行时支持全局快捷键打标签。
- 支持 Windows 资源管理器右键菜单打标签。
- 标签可以交叉、多选、重名；后台必须可靠区分同名标签。
- 提供类似 Windows 文件资源管理器的浏览和组织界面。
- 一个对象可同时拥有 A、B 等多个标签。
- 一个对象可同时标注父标签 A，以及 A 下的子标签 B。
- 支持 A/B/C 与 D/F/C 等多条嵌套路径；末端 C 可复用同一标签实体，也可为外观不同的独立同名实体，UI 必须可区分。
- 标签层级近似无上限。
- 文件浏览结果与标签层级上下文关联。
- 标签可新增、修改、删除。
- 若 TAGTAG 管理过程移动了文件，清除全部标签后应能恢复原位置。
- 支持检索、最近项目和常用标签。
- 支持多个独立标签空间，以及空间级备份和迁移。
- tagLyst Next 是用户期望功能完整度最接近的参考产品，且已在本机安装。

## 2026-08-09 现状审计（进行中）
- 用户已明确校正核心存储语义：TAGTAG 必须先初始化存储路径，否则不可使用；资源（用户口中的“文件”包含文件夹）默认复制到该路径，也可设置为移动。
- TAGTAG 管理范围内每个资源只维护一个物理实体；标签路径或其他组织位置使用虚拟引用/索引关系，不要求在磁盘上维护多份文件、快捷方式或链接文件。
- 文件名是否变化取决于设置或业务规则；文件预览不需要，导入/移动/改名等物理操作需要日志与撤销；独立“物理整理”功能不需要。
- “默认复制”仍会在源位置留下外部原件；当前暂按“源原件不属于 TAGTAG 管理范围，存储根内副本是唯一受管实体”理解，需用户确认。
- 用户确认整个应用使用一个全局存储根；多个标签空间是独立的标签、查询和历史视图，可以共同引用同一个受管实体，不建立各自的物理存储根。
- 用户确认未标注资源仍是合法受管资源，需要独立且关键的“待整理区”页面；它可以重新加标签或退出管理。“孤立内容”仅指存储根中有物理内容但数据库无对应记录的异常。
- 退出管理有三种方式：恢复到先前路径、移动到指定位置、按平台规则删除；Windows 首版进入回收站。清空标签默认只删除关系，也可进一步选择退出管理。所有退出操作写日志并支持撤销。
- 用户确认恢复先前路径时绝不自动覆盖：目标空闲才直接执行；同名冲突只能重命名后放回、改到其他位置或取消。
- 用户要求监控全局存储根：发现非 TAGTAG 操作造成的新增孤立内容时告警，由用户决定接管或移出；发现受管资源被外部删除时告知异常删除。
- 用户确认监控细则：正常内容/时间戳变化只更新元数据、不告警；外部改名或移动时让用户接受新路径或恢复记录路径；外部删除保留标签、历史和原路径并标记缺失，直到重新定位或清除记录。
- 用户确认复制导入是一次性边界：外部源资源保留但不再跟踪或同步，存储根内版本是 TAGTAG 唯一管理和后续编辑的版本。
- 用户确认默认保留原文件名，标签操作不改名，只有显式改名或启用命名规则才可修改；名称冲突按操作系统同一父目录规则处理，不同文件夹允许同名，且不得自动覆盖。
- 用户确认备份分层：全局备份包含全部数据与资源用于灾难恢复；空间导出包自包含该空间引用的资源内容并在包内去重，导入时复用已有实体；不含资源内容的标签配置称为空间模板而非备份。
- 用户明确唯一标签与独立同名标签的浏览语义：唯一标签在多条路径下共享同一实体和资源集合，从任一路径都自动看到全部关联资源；非唯一情况下，同名 C 是不同实体，资源完全隔离。当前原型按 `placementId` 统一筛选，不满足前一种行为。
- 用户确认唯一性作用于当前标签空间的标签身份：新建标签默认不唯一；显式复用已有标签才形成共享实体。不得用普通开关隐式改变，已有标签关系通过可预览、写日志且可撤销的合并/拆分操作调整。
- 用户确认文件夹标签默认只标文件夹本身；子项继承必须显式启用且为动态有效标签，覆盖当前和未来子项，移出后失去；继承来源需可见，批量直接标注当前子项是独立固定操作。
- 用户确认四种导入/标注入口：系统 Quick Tag、Explorer 右键、主窗口拖入和可选悬浮球拖入。未受管资源在同一窗口完成复制/移动与标签选择，一次确认或整体取消；各入口复用同一流程，不使用二段窗口。
- “已受管资源直接进入标签选择”适用于用户从全局存储根或其他 TAGTAG 视图再次选中已有实体的情况，此时不得重复导入。
- 用户确认导入并标注窗口允许零标签确认，资源进入待整理区；只有取消才表示不执行复制/移动或创建管理记录。
- 用户确认空间成员关系与标签关系分离：资源可属于多个空间；空间待整理区按该空间是否有有效标签计算，即使其他空间有标签仍可显示；全局待整理只汇总在所有空间都无有效标签的资源。
- 用户确认导入落盘规则：单一导入窗口选择或新建存储根内目标文件夹，默认目标可配置、未配置用根目录；文件夹导入保留自身与完整内部层级，多文件保留名称，标签不改变物理目录，禁止导入到存储根外。
- 用户最终撤销此前关于全局内容寻址、透明去重、内容对象共享和写时复制的要求，首版均不实现。每个受管实体与存储根中的一个具体文件或文件夹路径对应，文件编辑始终直接作用于该 Windows 普通文件。
- 多标签、多空间和多处组织只复用同一个受管实体 ID，不复制该实体的文件；必要的落盘入口可使用快捷方式或链接。不同物理路径即使内容相同也保持为不同受管实体和不同文件，不做内容级合并。
- 用户确认首版检索名称、受管路径、标签和基础元数据，并支持标签 AND、OR、NOT；不读取文件正文、不做内容提取，也不建立全文索引。
- 用户确认删除标签位置的保守语义：只删除该层级出现，子位置默认提升到父级，其他位置及资源标注不变；最后一个位置必须转入标签实体删除确认，删除整棵子树是独立、可预览且可撤销的命令。
- 用户确认删除标签实体时删除当前空间中的全部位置、直接标注和文件夹继承规则，并提升子位置；不移动或删除资源文件、不移除空间成员关系，失去最后一个有效标签的资源进入空间待整理区。操作需预览、记录日志并支持撤销。
- 用户确认继续使用 Flutter，不迁移到 .NET UI 技术栈；SQLite、标签模型和文件工作流留在应用核心，Windows 系统能力采用最小原生桥接，Explorer Shell 扩展保持独立且职责单一。
- 用户澄清 SQLite 可以用于标签、路径、索引、日志等元数据建表；强制约束是存储根内的资源文件保持原始、未加密的 Windows 普通文件，文件字节不得写入数据库、加密容器或应用专用包，编辑始终直达该物理文件。
- 开发审计确认当前 `LocalStore` 首次加载会返回 `AppState.demo()`，直接覆盖单个 JSON；`TagTagController` 在持久化成功前先替换内存状态并通知 UI；`TagResource.spaceId` 又把资源固定在单一空间。这三点必须由里程碑 1 的 `ManagedLibrary` seam 统一修正。
- 目标远程 `git@github.com:yuesewuta/TAGTAG.git` 可通过 SSH 访问，`origin/main` 当前指向 `b7b849022644105b89f81c32be712e71e426143a`；已只读获取为远程跟踪分支，本地仍无提交且未合并。
- 当前仓库是单包 Flutter Windows 项目，无 Git remote、无 commit 历史；所有项目文件目前均为未跟踪状态。
- 现有方案文档推荐 `.NET 8 + WinUI 3 + 后台 Agent + SQLite + Shell Bridge`，实际原型采用 Flutter + 内存控制器 + `%APPDATA%/TAGTAG/state.json`。两者不是同一交付阶段，不能把“Flutter 原型构建成功”等同于“Windows 核心 MVP 完成”。
- README 已明确承认系统级全局热键、Explorer 右键、目录选择/自动索引/文件监控、SQLite 大规模存储、物理整理与回滚均未实现。
- 已实现的主要价值是验证 `tagId` 与 `placementId` 分离、独立同名/同一实体复用、多位置标注、多空间的最小交互，以及 JSON 备份往返。
- 当前 `pubspec.yaml` 除 Flutter SDK 和测试/lint 外没有业务依赖，说明尚未接入数据库、Windows API、文件选择、文件监控、托盘、预览或 Shell 集成库。
- setup 技能初次探索时：无 Git/GitLab 远程、无 `AGENTS.md`/`CLAUDE.md`、无 `CONTEXT.md`/`CONTEXT-MAP.md`、无 `docs/agents`、无 `.scratch`，且未安装 `triage` 技能；项目无 monorepo 信号，因此 domain docs 采用 single-context。随后已按用户确认创建 `AGENTS.md`、`CONTEXT.md` 和 `docs/agents/*`。
- setup 技能在写入前仍需用户决定 issue tracker，并在 `AGENTS.md` 与 `CLAUDE.md` 中选择一个新建；`triage` 未安装，所以不配置 triage 标签。
- 控制器支持创建空间、创建/复用/改名/删除标签位置、手工添加资源、批量打标签、清除标签、最近资源、常用位置和 JSON 备份恢复；没有资源删除/编辑、空间删除/改名、路径有效性验证或实际打开文件逻辑。
- 当前检索只有资源名称/路径的大小写不敏感子串匹配，以及“一个选中标签位置 + 可选后代”的过滤；未实现标签 AND/OR/NOT、全文检索、查询 chip、保存查询、跨空间查询或“同一标签实体全部位置”模式。
- “常用标签”按所有带 `placementId` 的 usage event 简单计数并取前 5，没有时间衰减、固定/隐藏/清空历史；“最近”只来自本应用记录的打开/打标签事件，不是文件系统最近项目。
- 测试共 4 个，覆盖同一实体多路径/独立同名、父子直接标注去重、清标签不改路径、JSON 往返。尚无 UI、标签删除级联、跨空间隔离、循环与深层树、非法/损坏备份、原子写入或规模性能测试。
- `LocalStore.save` 直接覆盖 `state.json`，备份只是 JSON 副本，没有临时文件 + 原子替换、schema version、manifest、校验和、轮换或恢复前自动备份实现；README 中“恢复前自动创建当前数据备份”的表述需要核对 UI 调用链，不能仅凭存储层认定。
- UI 恢复流程会先调用 `createBackup()`，所以 README 的“恢复前自动备份”在正常 UI 路径成立；直接调用控制器 `restoreBackup()` 则不会自动备份，规则尚未封装在领域操作内部。
- 当前资源行的“打开”动作只记录 `opened` 事件并弹出提示，不会调用系统打开文件或在 Explorer 中定位；资源的 `modifiedAt` 也是添加记录的时间，不是真实文件修改时间。
- “最近使用”先把 usage event 转成资源 ID 集合，最终仍按资源 `modifiedAt` 排序，因此界面并不按最近事件时间排序。
- 首次启动在没有 `state.json` 时自动加载固定 demo 数据，而不是空工作区或显式示例空间；这可能让用户误把演示路径当作真实索引结果。
- 标签 CRUD 目前是“标签位置”的创建/删除和“标签实体”的改名/改色：没有位置移动/排序、实体级删除、合并、软删除或删除影响预览。原始“标签可删除”需要明确删除位置还是删除实体所有复用位置。
- 文件夹标签继承、直接标注与有效标注的区分只存在于方案文档，模型与 UI 尚未实现。
- `AppState.fromJson` 写入 `version: 1` 却不读取或校验版本，也不验证外键/空间一致性；损坏或未来版本 JSON 可能导致启动崩溃或后续 `firstWhere` 异常。
- 控制器先更新内存并通知 UI，再等待 JSON 保存；保存失败时界面仍显示未持久化的新状态，且多个未等待操作可能竞争覆盖文件。生产存储需要事务/串行写入与失败回滚语义。

## 初始判断（待证据验证）
- 需求同时包含“标签实体复用”和“同名独立标签”，数据模型不能用标签名称作为主键。
- “文件浏览和标签层级有关”适合实现为虚拟视图；默认不移动原文件可显著降低恢复、链接失效和跨卷移动风险。
- 若确需物理归档，应作为显式操作并记录可逆操作日志，不应成为普通打标签的默认副作用。

## 研究发现
- 当前工作区为空，不是 Git 仓库，也没有既有代码或文档。
- 本机卸载注册信息确认安装的是 `tagLyst 4.752`，发布者为 `Shanghai Fenzhi Management Consulting Co.,Ltd`，主程序为 `D:\tagLyst\run\tagLyst.exe`；正在运行的主窗口标题是 `tagLyst Next V4.752`。
- localtags 官方仓库将产品定位为“本地文件备份工具”，主要能力为文件备份、定期完整性检查、标签管理、文件历史版本和本地 Markdown 图库。
- localtags 是 Go + SQLite 的本地服务，以 `http://127.0.0.1:53549` 网页作为 GUI；Windows 提供可执行文件，视频缩略图可选依赖 ffmpeg。
- localtags 的核心分类约束是“纯标签管理，完全没有文件夹”，并要求每个文件至少设置两个标签；标签组用于维持分类秩序。
- localtags 的文件导入不是网页上传：用户把文件放入指定本地文件夹，再刷新页面获取列表；部分低频功能需要在浏览器控制台执行命令。作者明确将这些做法解释为降低代码量。
- localtags 最新仓库提交为 2021-11-27，最新 Release 为 2021-11-23；截至 2026-08-07 可视为长期未维护，不能直接作为现代 Windows 桌面架构基础。
- localtags 与 TAGTAG 的主要冲突：它围绕“复制/归档到备份仓库”而非“原地索引”，放弃文件夹语义，不以文件夹为标注对象，也没有证据表明支持全局快捷键或 Windows 右键菜单。
- localtags 可借鉴点：标签预览、标签组、重复文件/替换、文件完整性校验、历史版本，以及本地优先的数据控制。
- TagSpaces 官方定位是支持标签的离线、开源文档管理器，桌面端采用 Electron，仓库以 TypeScript 为主，并同时提供 Web 和 Capacitor 移动端构建。
- TagSpaces 仓库截至 2026-08-07 仍活跃：develop 分支最近提交为 2026-07-25，最新 Release `v6.13.12` 发布于 2026-07-20；约 10,400 commits、5.2k stars、513 forks。
- TagSpaces 支持扩展模块来查看/编辑不同文件类型，适合作为 TAGTAG 预览面板和插件边界的参考。
- TagSpaces README 明确提示：对单个 location 中超过 100,000 个文件的场景未优化。这是 TAGTAG 索引架构必须专门验证的规模门槛。
- TagSpaces 采用双许可：开源部分为 AGPLv3，也提供商业许可；`tagspacespro/` 组件是专有软件并受独立 EULA 约束。可以研究交互和公开接口，但若 TAGTAG 不采用 AGPL，不能无条件复制其实现代码。
- TagSpaces 官方文档设有独立的“Organizing Files and Folders with Tags”章节，确认其产品语义明确同时覆盖文件与文件夹；具体存储和层级能力待继续核对正文。
- TagSpaces 不依赖中央数据库保存标签。文件标签有两种模式：重命名为 `原名[tag1 tag2].ext`，或写入同目录隐藏 `.ts` 文件夹中的同名 `.json` sidecar。
- 文件名标签模式便携、跨同步工具仍可见，但会改变文件路径，并受 Windows 约 256 字符路径长度约束；这与 TAGTAG 的“近似无限标签/层级”和无感集成不兼容。
- sidecar 模式保持原文件名且标签数理论上不受限，但若用户在 TagSpaces 外移动或重命名文件，必须同步维护 sidecar；某些云盘默认也不复制隐藏 `.ts` 目录。
- TagSpaces 的文件夹标签固定写入文件夹内 `.ts/tsm.json`。它还支持批量文件/文件夹加标签、清空标签、逗号批量输入、属性面板标注，以及把标签拖到对象、把对象标签拖到另一对象。
- TagSpaces 说明文件和文件夹使用相同的多选批处理交互，这与 TAGTAG 的统一对象模型吻合。
- TagSpaces 的 Tag Library 以“标签组 → 标签”组织，标签组可折叠、排序、改名、着色、删除，并支持以 JSON 导入/导出；官方文档没有显示标签组继续嵌套或标签构成多级有向图，因此不能满足 TAGTAG 的近似无限层级。
- 标签库可以按组搜索“拥有组内至少一个标签”的文件/文件夹；Smart Tags 提供日期、时间和地理位置语义，Location Tags 可把标签组保存在 location 的 `.ts` 中随目录迁移，但 Location Tags 标为 Pro 功能。
- TagSpaces 建议标签名不含空格、反斜杠、斜杠或 `#`，以 `-`/`_` 替代。这是文件名存储模型泄漏到产品语义的限制，TAGTAG 使用 ID + 数据库后无需继承该限制。
- TagSpaces 的标签颜色与组颜色可用于快速识别，但“同名标签是否为同一实体”仍主要由名称/标签库语境表达，不足以覆盖 TAGTAG 的同名独立实体与跨路径复用要求。
- TagSpaces 搜索按文件/文件夹名称、描述、父目录路径、标签及标签描述加权；可选索引纯文本内容，PDF/DOCX 等办公格式全文检索标为 Pro。
- TagSpaces 标签查询原生支持 `AND`（`+tag`）、`OR`（`|tag`）、`NOT`（`-tag`），并可组合自由文本、文件类型、大小、创建/修改日期、当前目录/当前 location/全局范围、模糊度等过滤条件。
- TagSpaces 提供跨 locations 的全局检索、可配置的搜索历史，以及与 location 无关的保存查询；保存查询的 JSON 导入/导出为 Pro 功能，并使用唯一 ID 识别重复/新版查询。
- TAGTAG 应借鉴查询表达力，但查询 chip 内部必须保存 `tag_id` 和可选路径上下文，而不是只保存显示名，否则同名独立标签会合并。
- 已开始用 GitHub 仓库搜索定位“DeClutter + tag + files”，当前只确认存在同名歧义，尚未将任何结果认定为用户所指软件。
- GitHub 对 `DeClutter tag files` 的仓库搜索只有两个直接结果：`midnightdim/declutter` 的描述为“File tagger and organizer”，与本次语境高度吻合；`DesktopShelves/.github` 是桌面 shelf 分组产品，相关性较低。因此暂将前者认定为用户所指 DeClutter，并在最终报告保留识别说明。
- `midnightdim/declutter` 搜索页显示为 Python 项目、约 68 stars、最近更新于 2026-02-13；具体 README 功能仍待核对。
- DeClutter README 确认它是 PySide6 桌面应用，提供自动文件处理与带标签的简易文件管理器；使用 SQLite，主要在 Windows 测试，macOS 可运行但有限制，许可证为 MIT。
- DeClutter 标签器同时支持文件与文件夹、自定义标签、按标签筛选、音视频/图片预览，并可与系统文件资源管理器相互拖放。
- DeClutter 的标签组是“互斥组”：同一组内只允许一个标签附着到文件。这对状态/优先级有用，但若作为全局标签语义会与 TAGTAG 的任意多标签、重复/交叉标注冲突。
- DeClutter 自动化规则可用文件夹或带标签文件作为来源，按名称、大小、日期、标签、类型过滤，再执行删除、回收、复制、移动、重命名或加标签；目标路径可引用文件类型和指定标签组的首个标签。
- DeClutter 用例包括清理 Downloads、按条件移动、用标签表示过期时间、保留项目最近 N 个版本，以及用标签快速导航。其亮点是规则引擎，不是嵌套标签。
- DeClutter README 没有列出全局快捷键、Windows Explorer shell 右键、嵌套标签、多个标签空间、备份迁移或移动回滚；最终矩阵应标为“未见官方证据”，而不是武断宣称绝对不支持。
- DeClutter 仓库最新 Release 为 `v1.13.7`（2026-02-12），109 commits、单一主要贡献者，维护风险高于 TagSpaces。
- DeClutter 当前源码已拆分为 `db.py`、`store.py`、`tags.py`、`sidecar.py`、`rules.py` 和版本化 migrations，并保留旧 `src/` UI 层；这表明其标签数据同时涉及 SQLite 与 sidecar，具体一致性策略待继续核对。
- DeClutter 只有两个显式回归测试文件（scheduler flag 与特殊字符）；对其自动移动/删除规则而言，测试覆盖面偏低，不宜直接复用为 TAGTAG 的文件操作核心。
- DeClutter `sidecar.py` 在每个目录建立隐藏 `.dc`，每个文件或文件夹对应 `.dc/<对象名>.json`，JSON 只有 `{"tags": [名称...]}`；查询与增删都按标签名称字符串处理，因此无法区分两个同名 C，也无法表达一标签多路径。
- DeClutter sidecar 与对象名称绑定，外部移动/重命名面临与 TagSpaces 相同的一致性问题；它使用 Windows `attrib` 隐藏目录和 JSON，进一步增加平台耦合。
- DeClutter 遇到损坏 JSON 或找不到对应对象时会在遍历中直接删除 sidecar，且源码注释自己标记为“不太安全”。TAGTAG 必须采用 quarantine + 告警 + 修复日志，禁止静默删除唯一元数据副本。
- DeClutter `add_tags_sc` 通过 Python `set` 去重后回写，标签顺序不稳定；这也说明“重复 tag”在该产品中只能理解为多标签，不能表达用户要求的路径上下文重复。
- DeClutter schema v2 在 SQLite 中用整数 `tag_id` 连接自动化规则，说明规则内部比 sidecar 更结构化；但 legacy 导入仍通过 `SELECT id FROM tags WHERE name=?` 全局按名称归并，不能保留跨组同名独立标签。
- DeClutter 数据库包含 `recent_folders`，但这是用户手动排序的最近目录列表，不等同于完整的最近文件/最近标签使用统计。
- DeClutter 的规则 schema 已考虑 `dryrun`、保留标签、保留目录结构、覆盖策略、忽略最新 N 个文件等防护语义；TAGTAG 的物理移动功能应至少达到相同层次，并进一步增加事务日志与撤销。
- DeClutter `tags.py` 确认标签有整数 ID，但公共 API 仍普遍按名称查询/更新；把标签改名为已存在名称时会合并关联并删除旧实体。这一语义无法让用户选择“同名但不同 C”。
- DeClutter 中一个文件-标签关联只有一次：`set_tags` 先删除全部关联再按标签集合写回；按多标签搜索采用集合交集（AND）。标签组只是两层树，移动 API 只能“标签进组”和组间排序。
- DeClutter 用规范化小写路径标识对象，并保存 quick fingerprint；对象丢失时在设置根目录内最多深度 3 扫描指纹以尝试重定位。这优于纯 sidecar，但指纹碰撞、扫描成本、文件夹身份和跨卷场景仍需更强设计。
- 本机 tagLyst Next 安装内容确认是 Electron/Chromium 桌面应用（`app.asar`、Chromium resources、多个渲染进程），不是原生 WinUI 应用。
- tagLyst 示例把普通标签直接编码为文件名中的 `#标签` 后缀，星标为 `#$STARRED`；示例文件普遍同时带多个 `#标签`。
- tagLyst 每个示例资料库含 `$$TGL.DATA` 元数据目录，其中可见 `$$TGL.tagDB.json`、`$$TGL.superTagDB.json`、标签分隔符、资料库标识、封面/缓存和目标路径等文件；这说明资料库是相对自包含、可整体迁移的空间单元。
- tagLyst 支持 `.tglnk` 链接条目，安装资源图标还显示 Explorer 打开、备份、导出、恢复、继承标签、包含子目录、普通/列表/卡片/流式/文档/统计等多种视图线索。图标只证明 UI 中存在相应概念，具体行为仍需配置和文档验证。
- tagLyst 文件名标签同样会带来路径长度、外部程序引用失效、同名标签身份不足等问题；其 `superTagDB` 可能补充“超级标签”语义，待读取示例元数据确认。
- tagLyst 自带 readme 将功能概括为文件标签、直观浏览、离线使用、本地/私有云存储及兼容多文件系统；安装目录 URL 指向 `www.tagLyst.com`。
- tagLyst `tagDB` 以标签名称作为 JSON 对象键和 `tagKey`。字段包含 `groupNameArray`、`orderFromGroups`、颜色 CSS class、图标 class、别名、anti-tag、排序和样式开关；组记录以 `_tagGroup:<名称>` 为键，支持 `isOnlyOneChecked` 与 `isSubTagGroup`。
- tagLyst 的嵌套通过“同名标签 + 同名子组”表达：例如“经济管理”既是“书籍分类”下的标签，又是容纳“经济学/创业/市场营销”的子组。标签记录的 `groupNameArray` 为多父上下文预留结构，`orderFromGroups` 还记录 `(最近使用)` 的独立排序。
- 该模型能把同一个名称标签复用到多个父上下文，也能显示颜色/图标/别名，但名称是全局键，所以不能同时存在两个独立且同名的 C；标签实体与层级容器同名耦合也使改名和消歧复杂。
- `superTagDB` 示例按显示名保存特殊条目，并记录其实际文件相对路径；更像星标/固定入口索引，不是通用标签层级数据库。
- tagLyst 示例中确实存在名称带多个 `#标签` 的文件夹，确认其文件夹也可标注，而不仅是文件。
- tagLyst 资料库同时包含标签 DB 和文件名内嵌标签，具备便携性，但必须处理两份表达的一致性；TAGTAG 应避免把显示名同时作为身份键、层级节点键和文件名编码。
- 对“团队图书馆”示例的结构化统计为 23 个标签、7 个组，其中 2 个互斥组、3 个子组；该示例没有真正多父标签、别名或 anti-tag 实例，说明字段存在不等于常用流程已充分验证。
- tagLyst `app.asar` 包含 `tray.js`，并依赖 `auto-launch`、`winreg`、`fswin`，可确认它具备系统托盘/开机启动与 Windows 文件系统/注册表集成基础；是否注册全局快捷键或 Explorer 右键仍需在源文件/注册表中核对。
- tagLyst 包含文本抽取、PDF、Office/Excel、Markdown、代码高亮、拼音等依赖和多种文件类型资源，解释了其文档预览/内容视图与中文检索能力基础。
- tagLyst 前端包含旧版 Vue、UIkit 和大量直接打包依赖，`app.asar` 文件列表超过一万项；其产品语义值得参考，但不适合直接沿用这套依赖与单体打包结构开始新项目。
- 本机 tagLyst 4.752 在 HKCU/HKLM 常见的 `Classes\\*\\shell` 与 `Classes\\Directory\\shell` 注册表位置没有 tagLyst 项，未发现传统静态 Explorer 右键菜单安装证据。
- 当前证据只能确认 tagLyst 有托盘/自启动基础；全局快捷键和资源管理器右键必须在选择性读取 `main.js`/`tray.js` 或实际 UI 后才能定论。
- 选择性读取本机 tagLyst `main.js` 后确认 Windows 全局快捷键：`Super+Ctrl+T` 显示/隐藏主窗口，`Ctrl+Alt+0` 控制后台窗口；当 `clipAgent` 模式启用时，`Ctrl+Alt+T` 显示/收起置顶快速窗口。可用 `-noshortcutkeys` 或环境选项禁用。
- tagLyst 关闭主窗口时默认阻止退出并隐藏，托盘菜单可恢复窗口或退出；符合后台常驻产品形态。
- `Ctrl+Alt+T` 只在 `clipAgent` 模式注册，目前尚未确认其输入来源和是否能对 Explorer 当前选择直接打标签；能力矩阵应标为“有全局快捷入口，直接打标签部分验证”。
- tagLyst 主窗口启用 `nodeIntegration`、关闭 `contextIsolation` 并允许 remote module，这是旧 Electron 的高风险配置；TAGTAG 必须采用隔离 renderer + 最小 IPC allowlist，不能复制该安全模型。
- 本机设置文件给出官方快速上手文档 `https://www.yuque.com/taglyst/tgln-docs`，页面标题可访问为“taglyst Next 上手指南”。
- tagLyst 官方上手指南包含 12 篇约 33,862 字文档，覆盖资料库、文件操作、标签、筛选检索、便捷功能、局域网/云同步协作、FAQ、插件和更新日志；其产品表面明显比另外三款更接近用户的综合目标。
- 已定位官方“文件标签维护”正文页，后续用其核对嵌套/重复/互斥/继承标签的正式语义。
- tagLyst 标签不仅来自手动写入，也可由预设标签规则对文件名进行智能匹配；规则支持同义词和 `/.../` 正则。所谓“屏蔽型标签”实际提供排除条件（NOT），适合组合检索。
- tagLyst 支持批量选择对象后点选标签、手动新标签、复制一个对象的全部标签再粘贴到其他对象；预设标签可设置名称、同义词、颜色、图标、仅文字/仅图标样式和排序。
- 官方明确说明：预设标签删除或改名不会改变已经存在于文件上的标签，只改变标签库。这证明“标签定义”和“文件名内的标签字符串”弱一致，容易产生孤儿标签或同名语义漂移。
- tagLyst 从 3.6 起用“标签 A + 同名子标签组 A”间接实现多级：选中 A 后才显示 A1/A2。文档称可生成任意多层，但明确不推荐超过两层，因为维护成本高。
- tagLyst 4.6 起在智能匹配模式支持父标签继承匹配：选择父标签“太阳系”可匹配其下“地球/火星”等子标签。
- tagLyst 2.7 起允许一个标签同时属于多个标签组；这证明其交叉复用能力接近用户的 `A/B/C` 与 `D/F/C` 中同一个 C，但仍按名称识别，无法表示两个独立同名 C。
- 标签组可设为互斥，选中其中一个自动取消同组其他标签；这是适用于状态/GTD 的可选约束，不应成为 TAGTAG 普通组的默认行为。
- 每个资料库有独立预设标签库；可引用多个其他资料库标签库并与本地定义合并。引用的是只读副本，只能回原生资料库修改，尚不等同于带稳定跨空间身份的同步共享。
- tagLyst 标签文档标注适用于 V2.959+，页面更新时间为 2021-08-31；其中明确提到 V4.6，和本机 V4.752 基本同代，但仍需将文档事实与当前版本实测分开标注。
- tagLyst 筛选器可组合关键字、文件标签、文件类型、所在目录和文件时间，并提供精简/标准/高级三档复杂度。
- 默认标签逻辑是“同一分组内 OR、不同分组间 AND”；高级模式还提供恰好等于选中标签集合、包含全部选中标签、包含任一选中标签。加上屏蔽型标签，可覆盖 Exact/AND/OR/NOT 的主要语义。
- tagLyst 特殊标志包括星标、引用文件、无标签和废纸篓；这类系统状态不应伪装成普通用户标签，TAGTAG 应建独立系统属性与统一筛选 chip。
- tagLyst 文件夹标签默认向下继承给全部后代，也可关闭；官方说明新版文件夹标签不出现在文件夹名上但仍可检索文件夹及其后代。由此可见其文件夹标签另存元数据，旧示例的带 `#` 文件夹名不能代表当前默认存储。
- TAGTAG 必须区分 `direct assignments`（直接标注）和 `effective assignments`（由文件夹或标签层级继承），并在 UI 上给继承标签独立图标/来源路径，否则清除标签和查询结果会产生歧义。
- tagLyst 便捷功能文档集中列出快捷键、暗色模式和 `-new` 多实例启动；公开正文没有把 Windows Explorer 右键菜单列为功能，和本机注册表检查相符。
- tagLyst V3.3+ 默认单实例，但允许显式多开；TAGTAG 的“多个标签空间”更适合单进程多空间切换，而不是靠多个应用实例隔离状态。
- tagLyst 的“资料库”只是挂载用户指定的现有文件夹，可挂空目录或已有大量文件的目录，无需格式转换/导入；移除资料库仅移除路径引用，不删除或移动原文件。
- tagLyst 可同时挂载多个资料库并自由切换，每库有独立标签；资料库可自定义显示名、封面、排序和防误触锁，锁可分别限制标签库管理、打/清标签、打开/改名/删除文件。
- tagLyst 官方明确警告其文件名标签法会把 `合同.docx` 改为 `合同#已完成.docx`，对路径/文件名敏感者不应使用；这直接证明“便携”以路径稳定性为代价。
- tagLyst 每资料库可选择 `#`、`^`、`&`、`_` 四种分隔符，切换会改变整库文件名解释。这类空间级解析配置使迁移/误配置风险上升，不适合作为 TAGTAG 的核心标识机制。
- 对 TAGTAG 需求 12，最佳逻辑不是在普通打标签时移动文件再恢复，而是默认始终保留原路径；清空标签只删关联。只有用户显式执行“物理整理”时才记录 `old_path/new_path` 并提供回滚。
- tagLyst 的“同步/协作”不是自建云服务，而是让每个用户把同一 SMB、OneDrive/iCloud、Dropbox/坚果云或 Resilio Sync 目录挂为资料库，由外部文件级同步传递带标签文件名和资料库元数据。
- tagLyst 可直接挂载 UNC 路径或映射网络驱动器。TAGTAG 空间根路径因此需支持本地卷、可移动盘与 UNC，并明确离线/只读/权限不足状态。
- 官方同步文档未说明标签 DB 的并发合并、冲突检测或事务恢复。TAGTAG 若支持多机共享，不能只依赖“同步整个 SQLite 文件”；应采用单写者或操作日志/CRDT 风格同步，至少先把多机协作列为 MVP 之后能力。
- 空间备份与多人实时协作应拆开：前者可通过一致性快照 + manifest + 校验和可靠实现，后者还需要冲突协议和身份模型。
- tagLyst 可通过系统资源管理器直接向已挂载目录放文件，也可在软件内移动/复制实体文件；还支持创建不影响原文件本体的 `.tglnk` 引用和 URL 条目。
- tagLyst 提供卡片、传统列表、图片瀑布流、标签/文件夹统计、较大预览阅读流五种视图，并可切换只看当前目录或包含全部子目录。
- 文件列表支持 Ctrl/Shift/复选框多选，按时间、大小、标签数排序；操作包括打开、在 Explorer 中定位、移动/导出、废弃/删除、改标题、星标、封面、标记日期和克隆。
- tagLyst 软件内部对已选条目的右键操作不是 Windows Explorer shell context menu；竞品矩阵必须把两者分列。
- tagLyst 的实体文件导入会让用户选择移动或复制，但文档未说明自动保存原路径并提供一键撤销。TAGTAG 的物理文件操作必须先写 durable journal，再执行文件系统动作，并提供 dry-run/undo。
- “大幅预览”可用空格进入、Esc 退出，接近 macOS Quick Look；这是资源管理器式 UI 中值得保留的高频交互。
- tagLyst FAQ 建议单个资料库管理约 5,000 至 10,000 个文件；这不是数据库的理论上限，而是产品给出的体验边界。TAGTAG 不应沿用这一规模目标，至少要分别验证 10 万、100 万对象下的索引、分页和递归标签查询。
- tagLyst FAQ 说明，完整复制或同步整个资料库目录时，文件夹标签、预设标签库等 `$$TGL.DATA` 元数据可一并保留；这验证了“空间级自包含备份”对迁移体验的重要性。
- tagLyst 的 `.bak` 文件主要用于同步冲突或局部故障时的保护，并不等同于具有时间点、清单、校验和与恢复演练的完整备份系统。TAGTAG 应提供 SQLite 一致性快照、附件/sidecar manifest 和恢复校验。

## 技术决策
| 决策 | 理由 |
|------|------|
| 外部移动首版由用户显式配对缺失记录与孤立路径 | 现有元数据没有稳定的文件系统对象标识或内容摘要；按名称、大小、时间自动猜测会错误合并不同资源 |
| 指定位置退出使用明确的绝对目标路径 | 同一接口可表达文件夹、保留原名和用户选择“重命名后放回”；冲突检查仍按目标父目录的操作系统规则执行 |
| 退出目标禁止位于存储根内 | 资源记录删除后若物理内容仍在存储根会形成孤立内容，违反一致性安全边界 |
| `exit_move` 操作上下文保存最初导入路径 | 用户选择的退出位置不是资源的原路径；撤销后必须恢复原受管实体元数据，而不能把导出位置误记为原路径 |
| Windows 回收站撤销持久化 Shell item 的绝对 PIDL | 文件名和删除时间无法可靠定位同一回收站项；PIDL 可跨操作日志持久化，并由 Shell API 恢复文件或完整文件夹，同时避免直接操作 `$Recycle.Bin` 内部结构 |
| 以 UUID/ULID 作为标签实体主键 | 名称、颜色、父级和路径均可变化且可能重复 |
| 暂不把“无限层级”解释为数据库递归深度无限 | 产品语义可近似无限，工程上仍需循环检测、查询和 UI 性能边界 |
| 空间备份采用一致性快照而不是直接复制正在写入的数据库 | 防止 SQLite WAL 与文件元数据处于不同时间点，确保迁移后可校验、可恢复 |
| 用 `tags` 与 `tag_placements` 两层表达层级 | 同一实体可出现在多条路径，独立同名实体也可同时存在，标注还能保留路径上下文 |
| `assignments` 关联具体 `placement_id`，查询可切换位置/实体模式 | 避免给 C 打标签后无法判断命中 A/B/C 还是 D/F/C |
| Agent 是每个空间的唯一在线写入者 | UI、全局快捷键和 Explorer 右键不会并发修改数据库或产生多套业务逻辑 |
| Windows 首选 .NET 8 + WinUI 3，Shell 使用最小桥接组件 | 优先满足 Windows 视觉、无障碍、热键和 Explorer 深度集成 |

## 遇到的问题
| 问题 | 解决方案 |
|------|---------|
| DeClutter 名称可能对应多个 GitHub 项目 | 通过搜索结果、README 与用户需求上下文定位；无法唯一确认时在报告中显式标为歧义 |
| 首次追加 TagSpaces 发现时补丁上下文不存在 | 先读取当前文件，再按实际“资源”段落追加；未重复原补丁 |
| PowerShell 版本不支持 `ConvertFrom-Json -AsHashtable` | 改用 `PSObject.Properties` 遍历动态 JSON 属性，保持结构化解析 |

## 资源
- localtags 官方仓库：https://github.com/ahui2016/localtags
- localtags README：https://github.com/ahui2016/localtags/blob/main/README.md
- localtags Wiki：https://github.com/ahui2016/localtags/wiki
- TagSpaces 官方仓库：https://github.com/tagspaces/tagspaces
- TagSpaces README：https://github.com/tagspaces/tagspaces/blob/develop/README.md
- TagSpaces 官网：https://www.tagspaces.org/
- TagSpaces 标签文档：https://docs.tagspaces.org/tagging/
- TagSpaces 标签库文档：https://docs.tagspaces.org/ui/taglibrary/
- TagSpaces 搜索文档：https://docs.tagspaces.org/search/
- 暂定 DeClutter 官方仓库：https://github.com/midnightdim/declutter
- DeClutter README：https://github.com/midnightdim/declutter/blob/master/README.md
- tagLyst 官网：http://www.taglyst.com/
- tagLyst Next 上手指南：https://www.yuque.com/taglyst/tgln-docs
- tagLyst 文件标签维护：https://www.yuque.com/taglyst/tgln-docs/tags
- tagLyst 文件筛选检索：https://www.yuque.com/taglyst/tgln-docs/filter
- tagLyst 辅助及便捷功能：https://www.yuque.com/taglyst/tgln-docs/easy-use
- tagLyst 资料库管理：https://www.yuque.com/taglyst/tgln-docs/file-box
- tagLyst 同步与协作：https://www.yuque.com/taglyst/tgln-docs/sync
- tagLyst 文件基本操作：https://www.yuque.com/taglyst/tgln-docs/file-ops
- tagLyst FAQ：https://www.yuque.com/taglyst/tgln-docs/faq

## 视觉/浏览器发现
- localtags GitHub 页面显示 60 stars、6 forks、124 commits、4 个 releases；主要语言为 Go、HTML、JavaScript，页面最新代码活动停留在 2021 年。
- TagSpaces GitHub 页面的信息结构接近大型长期项目：桌面、Web、移动端、插件、测试和打包脚本集中在同一仓库；默认展示 develop 分支而非稳定分支。
- TagSpaces 标签文档显示三种视图可接受拖放标签：grid、list、kanban；属性区也能通过下拉或拖放添加标签。
- GitHub 搜索结果将 `midnightdim/declutter` 明确描述为文件标签与组织工具，是当前最可信的 DeClutter 身份匹配。

---
*每执行 2 次查看、浏览器或搜索操作后更新。*
