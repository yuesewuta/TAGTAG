# 02 - UI surfaces

State: resolved
Type: task
Blocked by: 01

## Comments

- Search page filter strip: 保存查询 action (disabled without an active condition, name prompt in a 420px `PrototypeDialogFrame` mini dialog) and saved-query chips with tap-to-apply + inline × delete; applying syncs the query text field.
- Hierarchy 标签操作 menu gained 固定到常用/取消固定 and 从常用隐藏/取消隐藏 with state-dependent labels and snackbar feedback.
- Quick Tag dialog section 最近使用 → 常用标签 showing `commonPlacements` when the search field is empty (falls back to all placements while no usage/pin data exists, so first-run tagging stays usable); text search still filters all placements.
- Status drawer history tab gained 清空历史记录 with an AlertDialog confirm step; operation log untouched.
- 4 widget tests in `test/saved_queries_ui_test.dart` pass; fixed a latent 2px vertical overflow in `PrototypeTagOption` (explicit line heights) exposed by the new Quick Tag test.
