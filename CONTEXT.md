# TAGTAG

TAGTAG 是一个把文件和文件夹导入受管存储，并通过标签和虚拟关系组织它们的本地资料库。

## Language

**资源**：
由 TAGTAG 管理的一个文件或文件夹。需求中未特别区分时，“文件”统一使用“资源”。
_Avoid_：用“文件”泛指文件与文件夹

**逻辑资源**：
TAGTAG 中拥有稳定身份、名称、受管路径、空间成员关系和标签关系的资源记录；它与存储根中的一个具体文件或文件夹对应。
_Avoid_：标签引用、按内容摘要合并的共享对象

**存储根目录**：
初始化 TAGTAG 时必须指定的全局唯一路径，所有受管资源存放于此；未完成初始化时软件不可使用。
_Avoid_：索引根目录、可选工作目录、空间目录

**导入**：
把外部资源纳入存储根目录的过程，默认复制，也可以按设置选择移动；复制完成后不再同步外部源资源。
_Avoid_：原地索引、仅登记路径

**导入并标注**：
在一个确认流程中选择复制或移动、选择可为空的标签集合并提交资源导入；零标签资源进入待整理区，取消时不产生任何受管文件或记录。
_Avoid_：先导入再弹出标签窗口、自动拖入即提交

**外部源资源**：
复制导入前位于存储根之外的文件或文件夹；导入后仍留在原处，但不受 TAGTAG 跟踪或同步。
_Avoid_：受管实体、同步副本

**受管实体**：
TAGTAG 管理范围内的逻辑资源记录，与存储根中的一个具体文件或文件夹对应；标签和空间引用该记录。
_Avoid_：标签副本、快捷方式副本、按内容合并的共享对象

**受管路径**：
受管实体在全局存储根目录中的持久 Windows 普通文件或文件夹位置，可由用户直接打开和编辑；名称冲突按当前操作系统在同一父目录内的规则判断，不要求跨文件夹全局唯一。
_Avoid_：标签路径、全局唯一文件名

**资源文件本体**：
存储根中的受管文件和文件夹必须保持为原始、未加密、可由 Windows 和原应用直接访问的普通文件系统内容。数据库只保存元数据和索引，绝不保存、封装或接管资源文件字节。
_Avoid_：数据库 BLOB、加密容器、应用专用文件包、只能经 TAGTAG 读取的内容

**存储投影**：
首版不使用虚拟磁盘或虚拟文件系统。每个受管实体对应一个持久的 Windows 普通文件或文件夹；标签及其他组织位置默认由数据库索引呈现，确需落盘时只使用快捷方式或链接，不为同一受管实体复制文件。
_Avoid_：临时工作副本、每个标签路径一份内容、虚拟文件系统

**导入目标文件夹**：
“导入并标注”时选择的存储根内物理目录；文件夹导入保留被导入文件夹及其完整内部层级。
_Avoid_：标签路径、存储根外路径

**虚拟引用**：
资源在标签路径或其他组织视图中的逻辑出现，指向同一个受管实体，不要求创建快捷方式或链接文件。
_Avoid_：文件副本、物理整理

**标签空间**：
全局存储根之上的独立标签、查询和历史视图；不同标签空间可以引用同一个受管实体，但不拥有独立的物理存储根。
_Avoid_：存储目录、资源副本、独立资料库

**空间成员关系**：
受管实体属于某个标签空间的事实，独立于该空间中的标签标注；同一受管实体可以属于多个标签空间。
_Avoid_：标签标注、物理副本

**标签实体**：
拥有稳定身份的概念标签；显示名不是身份，同名标签可以是同一实体，也可以是不同实体。
_Avoid_：标签位置、标签名称字符串

**标签位置**：
标签实体在标签层级中的一次出现；同一个唯一标签实体可以拥有多个标签位置。
_Avoid_：标签实体、独立同名标签

**删除标签位置**：
只删除标签实体在某条层级路径中的一次出现；子位置默认提升到被删除位置的父级，其他位置及资源标注保持不变。最后一个位置不能按普通位置删除，必须转入标签实体删除确认。“删除子树”是独立命令。
_Avoid_：默认级联删除子树、删除资源文件、静默删除整个标签实体

**删除标签实体**：
删除当前标签空间中该实体的全部位置、直接标注和文件夹继承规则；子位置提升到各自父级。资源文件和空间成员关系保持不变，失去最后一个有效标签的资源进入该空间待整理区。执行前必须预览影响，并写入日志、支持撤销。
_Avoid_：删除资源文件、退出资源管理、默认级联删除子标签实体

**唯一标签**：
在当前标签空间内由用户显式复用、因而由多个标签位置共享同一个标签实体和资源集合的标签；从任一位置浏览都能看到该实体关联的资源。
_Avoid_：名称碰巧相同的独立标签

**独立同名标签**：
显示名相同但身份不同、资源集合互不共享的多个标签实体；新建标签默认采用此独立身份语义。
_Avoid_：唯一标签的多个位置

**合并标签**：
把已有独立标签显式合并为一个唯一标签实体的受管操作，执行前必须展示资源和层级影响。
_Avoid_：按名称自动去重、切换布尔属性

**拆分标签**：
把唯一标签的一个或多个位置显式拆成独立标签实体的受管操作，执行前必须展示资源和层级影响。
_Avoid_：复制标签位置、普通改名

**直接标注**：
用户明确建立的资源与标签实体关系，不因资源所在文件夹变化而消失。
_Avoid_：继承标签、查询结果

**子项继承**：
文件夹标签可显式启用的动态规则，使当前和未来子项获得有效标签；子项移出该文件夹后自动失去该继承结果。
_Avoid_：批量直接标注、复制标签关系

**有效标签**：
资源的直接标注与当前子项继承结果的合集，并保留每个标签的来源信息。
_Avoid_：全部写入资源的直接标签

**全局备份**：
包含全部受管资源、数据库、操作日志、清单和校验信息的完整恢复单元。
_Avoid_：空间模板、仅元数据导出

**空间导出包**：
包含一个标签空间的标签、查询、历史、资源清单及其引用资源内容的可迁移单元；同一受管实体即使被多个标签引用也只打包一次，不按文件内容合并不同受管实体。
_Avoid_：全局备份、空间模板

**空间模板**：
只包含标签结构等可复用配置、不包含资源内容的空间初始化材料。
_Avoid_：备份、资源归档

**操作日志**：
记录会改变资源路径、名称或存在状态的受管操作，并为撤销提供依据。
_Avoid_：使用历史、最近项目

**未标注资源**：
没有任何标签关系、但仍由 TAGTAG 完整管理的受管实体；它可以重新标注或退出管理。
_Avoid_：孤立文件、未管理文件

**待整理区**：
集中浏览和处理未标注资源的关键页面；空间待整理区按空间成员关系和该空间的有效标签计算。
_Avoid_：回收站、异常列表

**全局待整理**：
汇总在所有标签空间中都没有任何有效标签的受管实体的视图。
_Avoid_：单个空间的待整理区、孤立内容

**孤立内容**：
存储根目录中存在物理内容、但 TAGTAG 没有对应受管实体记录的异常状态。
_Avoid_：未标注资源

**接管**：
为存储根目录中的孤立内容建立受管实体记录，使其正式进入 TAGTAG 管理范围。
_Avoid_：复制导入、重新下载

**缺失资源**：
TAGTAG 仍有受管实体记录、但对应物理内容已不存在的异常状态；标签、历史和原路径保留到用户重新定位或清除记录。
_Avoid_：退出管理、已删除记录

**一致性告警**：
存储根目录的物理状态与 TAGTAG 记录不一致时产生、需要用户处理的告警。
_Avoid_：普通通知、使用历史

**首版检索范围**：
检索资源名称、受管路径、标签和文件类型、大小、创建时间、修改时间等基础元数据，并支持标签 AND、OR、NOT 组合；不读取文件正文，不建立全文索引。
_Avoid_：正文全文检索、文档内容提取、文件预览

## 技术路线

- 继续使用 Flutter 实现 Windows 桌面应用、界面和业务核心，不迁移到 .NET UI 技术栈。
- SQLite 只保存标签、路径、索引、日志等元数据；资源文件本体始终保留在存储根的普通文件系统中，不进入数据库。
- 全局快捷键、Windows 回收站、文件监控等系统能力通过最小 Windows 原生桥接实现。
- Explorer Shell 扩展作为独立、最小的原生组件，只负责把选择项交给统一的 TAGTAG 工作流。

**外部结构变更**：
不是由 TAGTAG 发起的新增、删除、改名或路径移动；正常编辑导致的内容和时间戳变化不属于外部结构变更。
_Avoid_：内容修改、TAGTAG 受管操作

**退出管理**：
将受管实体移出 TAGTAG 管理范围的操作，可恢复到先前路径、移动到指定位置，或按当前平台规则删除。
_Avoid_：清空标签、停止索引
